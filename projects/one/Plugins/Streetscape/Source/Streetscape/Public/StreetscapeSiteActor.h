// StreetscapeSiteActor - placeholder (UE_PLAN.md 2.10). Filled in by the phase that owns that section; until then this header
// only exists so the file tree and includes are stable. No .generated.h include until a reflected type lives here
// (UHT emits nothing for a header without one and the include would fail).

#pragma once

#include "CoreMinimal.h"
